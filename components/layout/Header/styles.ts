import tw, {styled} from 'twin.macro';

export const Container = styled.header(() => tw`w-full bg-light p-4 h-16 flex items-center justify-between border-b border-solid border-gray-200 dark:border-none`);